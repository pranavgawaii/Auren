import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Settings — Auren",
  description:
    "Manage your integrations, reply tone, working hours, and GitHub repository settings.",
};

export default function SettingsPage() {
  return (
    <main>
      <h1>Settings</h1>
    </main>
  );
}
