import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Team | Auren",
  description: "Manage your team contacts and @mentions with Auren.",
};

export default function TeamPage() {
  return null;
}
