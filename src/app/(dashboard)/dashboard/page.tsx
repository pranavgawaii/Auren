import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Dashboard | Auren",
  description: "Your day at a glance — schedule, inbox, and recent commands in one place.",
};

export default function DashboardPage() {
  return null;
}
